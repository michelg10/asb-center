export class gNumber {
  id: string;
  gnumber: string;
  studentId: string;
  constructor(id: string, gnumber: string, studentId: string) {
    this.id = id;
    this.gnumber = gnumber;
    this.studentId = studentId;
  }
}